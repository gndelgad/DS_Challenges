# Description

Job that users Pyspark to read a large xml file with e-commerce sales orders, distributes the append of weather information, removes pii data and finally stores the final xml in Azure.

In order to run the script do:

$ python3 spark.py 'path_to_big_xml_file' 


